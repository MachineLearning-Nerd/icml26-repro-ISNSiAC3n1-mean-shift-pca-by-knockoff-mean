# Claim 2 evaluator contract

Run the fixed command `uv sync --frozen && uv run --no-sync python reproduce.py` on Hugging Face `cpu-upgrade`. The process exits nonzero unless the analytic contradiction, the finite-size counterexample, the centered negative control, all cumulative earlier verifiers, and the no-GPU check pass. Raw seeded rows, slopes, assumptions, CPU quota, runtime, Git SHA, and checker output are printed in the delimited evidence JSON.
