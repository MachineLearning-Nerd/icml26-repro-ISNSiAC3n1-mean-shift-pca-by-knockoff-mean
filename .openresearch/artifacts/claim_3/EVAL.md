# Claim 3B evaluator contract

Run `uv sync --frozen && uv run --no-sync python reproduce.py` on Hugging Face `cpu-upgrade`. The output exposes official commit, implementation deviations, every per-size rate, effective strength, independent-checker error, control output, seed, CPU quota, runtime, and Git SHA. The process exits nonzero on any failed cumulative verifier.
