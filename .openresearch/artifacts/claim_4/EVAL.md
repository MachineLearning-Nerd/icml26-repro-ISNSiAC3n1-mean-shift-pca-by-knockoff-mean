# Claim 4 evaluator contract

Run `uv sync --frozen && uv run --no-sync python reproduce.py` on Hugging Face `cpu-upgrade`. The current verifier prints every analytical step, exact spike location and derivative, source correction, applicability bridge, BBP-threshold control, prior empirical-route summary, CPU quota, runtime, and Git SHA. It exits nonzero unless all cumulative claim checks pass.
