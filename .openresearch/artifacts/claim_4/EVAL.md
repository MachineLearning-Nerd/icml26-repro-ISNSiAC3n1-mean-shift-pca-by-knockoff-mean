# Claim 4 evaluator contract

Run `uv sync --frozen && uv run --no-sync python reproduce.py` on Hugging Face `cpu-upgrade`. The verifier prints every raw eigenvalue, standard deviation, slope, bootstrap interval, scaled dispersion, wrong-rate control, independent-checker error, seed, CPU quota, runtime, and Git SHA. It exits nonzero unless all cumulative claim checks pass.
