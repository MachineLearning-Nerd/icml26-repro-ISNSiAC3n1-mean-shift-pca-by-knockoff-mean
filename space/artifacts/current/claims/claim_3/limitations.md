# Claim 3A limitations and deviations

This branch tests the paper-text eigenvalue algorithm at `c=1`; it does not claim universality over all aspect ratios or mixture weights. The released code compares singular values and multiplies the recommended knockoff norm by two, producing a fourfold strength change. That materially different implementation is reserved for a sibling experiment.
