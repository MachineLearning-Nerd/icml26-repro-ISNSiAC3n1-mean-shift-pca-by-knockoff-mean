# Claim 1 method

Use exact rational arithmetic at `c=1/2`, rank one, `ell=theta^2=3`, `pi=1/5`, and `||m||^2=15`. Both strengths exceed the BBP threshold and satisfy the theorem's domain. The theorem's own mapping gives `g(ell)=g(theta^2)=14/3`, so the two origins cannot be separated by their asymptotic locations.

An independent integer cross-product checks equality. A distinct-strength control (`theta^2=6/5`) must not trigger the counterexample and gives location `187/60`, separated from `14/3` by `31/20`.
